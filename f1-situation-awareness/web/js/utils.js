// js/utils.js

/**
 * Funzione segnaposto per la formattazione di un tempo in formato mm:ss:msmsms.
 * (Verrà implementata successivamente con la logica corretta).
 *
 * @param {number} tempoInMillisecondi - Il tempo da formattare in millisecondi.
 * @returns {string} - Il tempo formattato.
 */
function formattaTempo(tempoInMillisecondi) {
    // Al momento, restituisce solo il numero così com'è.
    return tempoInMillisecondi.toString();
}

// Esporta le funzioni per l'uso in altri file, se necessario.
// Al momento, le manteniamo globali come richiesto.